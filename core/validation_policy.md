# Validation Policy

Run `python3 tests/quick_validate_all.py` before release or before marking a module active.

Validation checks structure, module registry status labels, active module required files, script syntax, private-path patterns, and verified-template manifest hashes.
