# MDP Change History

Public release note: this repository includes only generic public templates. Historical local templates, private server provenance, and project-specific validation logs are intentionally excluded.

Rules:

- Do not edit MDP parameter values silently.
- Preserve disabled template lines when documenting changes.
- If parameters change, record a diff, rationale, and validation outcome.
- Confirm production length before running.
